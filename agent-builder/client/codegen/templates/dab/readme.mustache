# {{{agentName}}}

Generated by [Databricks Agent Builder](https://veenaramesh.github.io/dbrx-agent-builder/).

## Project structure

```
├── databricks.yml              # DAB bundle config + deployment targets
├── requirements.txt            # Python dependencies
├── src/
│   └── agent.py               # Agent implementation (MLflow ChatModel)
└── resources/
    └── agent_deployment.yml   # Registered model + serving endpoint
```

## Setup

```bash
pip install databricks-cli
databricks configure
pip install -r requirements.txt
```

## Deploy

```bash
# Deploy to dev
databricks bundle deploy

# Deploy to prod
databricks bundle deploy --target prod
```

## Components

{{#hasVectorSearch}}
- **Vector Search** — retrieves relevant documents before calling the LLM
{{/hasVectorSearch}}
{{#hasUCFunctions}}
- **UC Functions** — `uc_tools` list is available for tool-calling workflows
{{/hasUCFunctions}}
- **LLM** — `{{{llmEndpoint}}}`
- **Serving endpoint** — `{{{bundleName}}}-endpoint`
