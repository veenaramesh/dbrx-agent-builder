# Production build (Databricks App): the client is served same-origin as the
# API, so the API base must be empty. This must be set here explicitly —
# otherwise Vite falls through to .env.local (localhost:8001), which bakes a
# dead URL into the bundle and every API call fails with "Failed to fetch".
VITE_API_URL=
